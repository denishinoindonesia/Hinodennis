# =====================================
# 🔒 SECURITY + SEO CONFIG - ROOT
# =====================================

# 1️⃣ Disable directory listing
Options -Indexes

# 2️⃣ Force HTTPS
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# 3️⃣ Block sensitive files
<FilesMatch "(^\.|config\.php|koneksi\.php|\.env|\.git|composer\.json|composer\.lock|package\.json|yarn\.lock|\.htpasswd)">
    Order allow,deny
    Deny from all
</FilesMatch>

# 4️⃣ Block malware "detail/" links
RewriteCond %{THE_REQUEST} \?detail/ [NC,OR]
RewriteCond %{QUERY_STRING} detail/ [NC,OR]
RewriteCond %{REQUEST_URI} index\.php\?detail/ [NC,OR]
RewriteCond %{REQUEST_URI} /detail/ [NC]
RewriteRule .* - [G,L]

# 5️⃣ Hotlink protection (Google & Bing allowed)
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} !^https://(www\.)?official-hino\.com [NC]
RewriteCond %{HTTP_USER_AGENT} !(Googlebot|bingbot|Slurp|DuckDuckBot|YandexBot) [NC]
RewriteRule \.(jpg|jpeg|png|gif|webp)$ - [F,L]

# 6️⃣ Cache optimization
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
    ExpiresDefault "access plus 3 days"
</IfModule>

# 7️⃣ Security headers
<IfModule mod_headers.c>
Header set X-Content-Type-Options "nosniff"
Header set X-Frame-Options "SAMEORIGIN"
Header set X-XSS-Protection "1; mode=block"
Header always set Referrer-Policy "strict-origin-when-cross-origin"
Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
</IfModule>

# 8️⃣ Allow ALL bots to access robots.txt
<Files "robots.txt">
    Allow from all
</Files>
